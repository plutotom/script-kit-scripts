// Name: Kit reference holder

import "@johnlindquist/kit";

// tail -f ~/.kenv/kenv/plutotom/logs/test-cron-job.log
// await notify(`Stand up and stretch`);

// import "@johnlindquist/kit";

// let [color1, color2, color3] = await fields({
//   onChange: (input, state) => {
//     setHint(state.value);
//   },
//   fields: [{ type: "color" }, { type: "color" }, { type: "color" }],
// });

// await widget("<div>test</div>");

/// ############################################################################################################################################

// let formatProcesses = async () => {
//   let processes = await getProcesses();
//   return processes
//     .filter((p) => p?.scriptPath)
//     .filter((p) => !p?.scriptPath?.endsWith("processes.js"))
//     .map((p) => {
//       return {
//         name: p?.scriptPath,
//         description: `${p.pid}`,
//         value: p,
//       };
//     });
// };

/// ############################################################################################################################################

// await cli(
//     "set-env-var",
//     "TUTORIAL_CONTENT_PATH",
//     TUTORIAL_CONTENT_PATH
//   )
// kitPath("cli", "check-process.js");
// let temp = await run(kitPath("cli", "processes.js"));
// let temp = await run(kitPath("cli", "formatProcesses.js"));
// let temp = await run(kitPath("cli", "process.js/"));
// let formatProcesses = async () => {
//   let processes = await getProcesses();
//   return processes
//     .filter((p) => p?.scriptPath)
//     .filter((p) => !p?.scriptPath?.endsWith("processes.js"))
//     .map((p) => {
//       return {
//         name: p?.scriptPath,
//         description: `${p.pid}`,
//         value: p,
//       };
//     });
// };

// await dev(await getProcesses());

/// ############################################################################################################################################

setAlwaysOnTop(true);
setIgnoreBlur(true);

/// ############################################################################################################################################

/// ############################################################################################################################################

/// ############################################################################################################################################

/// ############################################################################################################################################

/// ############################################################################################################################################
await exit();
