// Name: Php pint formate this file
// Snippet: ppht

import "@johnlindquist/kit";

// await keyboard.pressKey(Key.LeftControl);
// await keyboard.pressKey(Key.Grave);

// await setTimeout(() => {
//   return;
// }, 1000);
await keyboard.type("./vendor/bin/pint --dirty");
// await setTimeout(() => {
//   return;
// }, 1000);
// await keyboard.pressKey(Key.Enter);
